# Playwright Utilities

Shared helpers for API calls, auth, and reporting.

| File (planned) | Purpose |
|----------------|---------|
| `apiClient.ts` | Conduit REST API wrapper (login, articles, users) |
| `auth.ts` | API login + JWT injection into browser storage |
| `allure.ts` | Allure metadata helpers (labels, severity) |
| `testUsers.ts` | Resolve credentials from env (never hardcode) |

<!-- TODO: Implement utilities — see .cursor/rules/playwright-standards.mdc -->
