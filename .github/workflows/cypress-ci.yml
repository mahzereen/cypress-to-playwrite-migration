name: Cypress CI

on:
  push:
    branches: [main]
    paths:
      - 'cypress/**'
      - 'infra/**'
      - '.env.example'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/cypress-ci.yml'
      - '.github/actions/setup-conduit/**'
  pull_request:
    paths:
      - 'cypress/**'
      - 'infra/**'
      - '.env.example'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/cypress-ci.yml'
      - '.github/actions/setup-conduit/**'
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

concurrency:
  group: cypress-ci-${{ github.ref }}
  cancel-in-progress: true

env:
  BASE_URL: http://localhost:3000
  CONDUIT_API_URL: http://localhost:3001/api
  TEST_USER_EMAIL: ${{ secrets.TEST_USER_EMAIL || 'test@example.com' }}
  TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD || 'TestPassword123' }}

jobs:
  cypress:
    name: Cypress E2E — Conduit
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: |
            package-lock.json
            cypress/package-lock.json

      - name: Start and seed Conduit
        uses: ./.github/actions/setup-conduit
        with:
          test_user_email: ${{ env.TEST_USER_EMAIL }}
          test_user_password: ${{ env.TEST_USER_PASSWORD }}

      - name: Install Cypress dependencies
        working-directory: cypress
        run: |
          npm ci
          npx cypress install

      - name: Clean prior Allure results
        run: rm -rf cypress/reports/allure-results && mkdir -p cypress/reports/allure-results

      - name: Run Cypress tests
        id: cypress-test
        working-directory: cypress
        env:
          BASE_URL: ${{ env.BASE_URL }}
          CONDUIT_API_URL: ${{ env.CONDUIT_API_URL }}
          TEST_USER_EMAIL: ${{ env.TEST_USER_EMAIL }}
          TEST_USER_PASSWORD: ${{ env.TEST_USER_PASSWORD }}
        run: |
          start=$(date +%s%3N)
          npx cypress run --config retries=0
          exit_code=$?
          end=$(date +%s%3N)
          echo "duration_ms=$((end - start))" >> "$GITHUB_OUTPUT"
          echo "exit_code=${exit_code}" >> "$GITHUB_OUTPUT"
          exit "${exit_code}"

      - name: Setup Java (Allure)
        if: always()
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Generate Allure report
        if: always()
        working-directory: cypress
        run: npm run report:generate

      - name: Upload Allure results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-allure-results
          path: cypress/reports/allure-results/
          retention-days: 30
          if-no-files-found: warn

      - name: Upload Allure HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-allure-report
          path: cypress/reports/allure-report/
          retention-days: 30
          if-no-files-found: warn

      - name: Upload failure artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-screenshots
          path: |
            cypress/screenshots/
            cypress/videos/
          retention-days: 7
          if-no-files-found: ignore

      - name: Stop Conduit
        if: always()
        run: npm run app:down
