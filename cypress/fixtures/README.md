# Cypress Fixtures

Static test data for Conduit scenarios.

| File (planned) | Purpose |
|----------------|---------|
| `users.json` | Test user profiles (emails only — passwords from env) |
| `articles.json` | Sample article payloads |
| `comments.json` | Sample comment payloads |

<!-- TODO: Add fixture files — never commit live credentials or JWTs -->
