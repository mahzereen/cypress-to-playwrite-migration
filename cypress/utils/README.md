# Cypress Utilities

Shared helpers for API calls, auth, and reporting.

| File (planned) | Purpose |
|----------------|---------|
| `apiClient.js` | Wrapper for Conduit REST API |
| `auth.js` | `cy.session()` login + API-login helpers |
| `allure.js` | Allure metadata helpers (severity, epic, feature) |
| `selectors.js` | Centralized selector constants (interim — migrate to POM) |

<!-- TODO: Implement utilities — see .cursor/rules/cypress-standards.mdc -->
